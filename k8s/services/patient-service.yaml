apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: medsync
  labels:
    app: patient-service
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    name: http
  selector:
    app: patient-service
