apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medsync
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    name: http
  selector:
    app: auth-service
