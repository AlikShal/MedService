apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: medsync
  labels:
    app: doctor-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: doctor-service
