apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: medsync
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    name: http
  selector:
    app: notification-service
