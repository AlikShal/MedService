apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: medsync
  labels:
    app: appointment-service
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    name: http
  selector:
    app: appointment-service
