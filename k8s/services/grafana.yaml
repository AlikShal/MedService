apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: medsync
  labels:
    app: grafana
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    name: web
  selector:
    app: grafana
